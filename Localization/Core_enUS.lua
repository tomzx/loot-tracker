local L = LibStub("AceLocale-3.0"):NewLocale("LootTracker", "enUS", true)
if L == nil then
    return
end

L["Debug"] = "Enable debugging"
L["DebugDescription"] = "Write debugging information to the chat log."